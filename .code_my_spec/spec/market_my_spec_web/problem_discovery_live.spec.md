# MarketMySpecWeb.ProblemDiscoveryLive

LiveView surface for the ProblemDiscovery feature: Frame composition (Frames index + new) and Frame detail with inline Board (Candidates table with kill buttons per story 739).

## Type

live_context

## Dependencies

- MarketMySpec.ProblemDiscovery
