# MarketMySpecWeb.FilesLive

Account-scoped file browser. Shows the artifacts written by the user's agent (skill outputs) for the current account, with read access. Files are shared state between the model (which writes via MCP) and the user (who reviews here).

## Type

live_context

## Dependencies

- MarketMySpec.Accounts
- MarketMySpec.Files
