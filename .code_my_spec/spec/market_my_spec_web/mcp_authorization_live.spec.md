# MarketMySpecWeb.McpAuthorizationLive

OAuth 2.0 consent screen. When an MCP client requests authorization, this LiveView shows the user what scopes are being granted and captures approve/deny. Posts back to the OAuth authorization endpoint with the user's decision.

## Type

liveview

## Dependencies

- MarketMySpec.Users
- MarketMySpec.McpAuth
