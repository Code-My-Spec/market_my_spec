# MarketMySpec.McpServers.ProblemDiscovery.Tools.RunGather

MCP tool: run Gather for a Frame, per-saved-search. Defaults to gathering only ungathered saved searches (additive); accepts an optional force flag to refetch. Returns N gathered + M failed per saved search.

## Type

module

## Dependencies

- MarketMySpec.ProblemDiscovery
