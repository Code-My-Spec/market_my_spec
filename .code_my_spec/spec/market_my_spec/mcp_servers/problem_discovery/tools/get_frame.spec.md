# MarketMySpec.McpServers.ProblemDiscovery.Tools.GetFrame

MCP tool: fetch a single Frame's artifact graph at a glance — Frame attributes plus per-stage artifact counts (JobPostings, Candidates, PaidJobSignals, RedTeamVerdicts).

## Type

module

## Dependencies

- MarketMySpec.ProblemDiscovery
