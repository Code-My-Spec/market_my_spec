# MarketMySpec.McpServers.ProblemDiscovery.Tools.RedTeamCandidate

MCP tool: prosecute one Candidate (story 741). Conversational with the agent, one candidate at a time. Takes the Candidate's evidence (member JobPostings + gated_in PaidJobSignals), produces a RedTeamVerdict that overwrites Score's verdict on the Board.

## Type

module

## Dependencies

- MarketMySpec.ProblemDiscovery
