# MarketMySpec.McpServers.ProblemDiscovery.Tools.GetBoard

MCP tool: fetch the assembled Board view for a Frame — Candidates with their final verdict (RedTeamVerdict overrides Score's verdict), the Frame's threshold values, and the kill_condition status (met / not met).

## Type

module

## Dependencies

- MarketMySpec.ProblemDiscovery
