# MarketMySpec.McpServers.ProblemDiscovery.Tools.UpdateFrame

MCP tool: update a Frame's description, money_gate threshold, kill_condition, or saved searches. Adding a saved search makes it eligible for the next per-saved-search Gather run.

## Type

module

## Dependencies

- MarketMySpec.ProblemDiscovery
