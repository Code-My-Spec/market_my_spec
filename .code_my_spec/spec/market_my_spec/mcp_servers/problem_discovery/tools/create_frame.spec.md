# MarketMySpec.McpServers.ProblemDiscovery.Tools.CreateFrame

MCP tool: create a Frame with description, saved searches, money_gate threshold (total_spent_min + hire_rate_min for JobPosting measurement), and kill_condition (min_money_gated_candidates).

## Type

module

## Dependencies

- MarketMySpec.ProblemDiscovery
