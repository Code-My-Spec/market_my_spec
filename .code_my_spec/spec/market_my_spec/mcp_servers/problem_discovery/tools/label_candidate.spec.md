# MarketMySpec.McpServers.ProblemDiscovery.Tools.LabelCandidate

MCP tool: assign a semantic label to a Candidate. Agent's labeling surface — Path C splits algorithmic grouping (MMS) from semantic naming (agent).

## Type

module

## Dependencies

- MarketMySpec.ProblemDiscovery
