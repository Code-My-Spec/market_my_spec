# MarketMySpec.McpServers.ProblemDiscovery.Resources.SkillOrientation

MCP resource exposing the problem-discovery skill orientation: enumerates the 6 pipeline phases (Frame, Gather, Cluster, Score, Red-team, Board), names the ProblemDiscovery MCP tools the agent invokes per phase, and distinguishes founder-direct LiveView surfaces from skill-driven agent flows. Mirrors MarketMySpec.McpServers.MarketingStrategy.Resources.SkillOrientation.

## Type

module

## Dependencies

- MarketMySpec.Skills.ProblemDiscovery
