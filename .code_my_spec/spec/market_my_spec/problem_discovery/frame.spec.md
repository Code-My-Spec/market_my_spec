# MarketMySpec.ProblemDiscovery.Frame

Founder-authored Frame artifact: description + list of saved searches (source + query) + typed money_gate threshold + structured kill_condition. Root of the per-Frame artifact graph; every downstream artifact (JobPosting, Candidate, PaidJobSignal, RedTeamVerdict) belongs_to Frame.

## Type

schema
