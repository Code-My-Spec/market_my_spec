# MarketMySpec.Files.Tools

Agent-facing file API — read_file, write_file, edit_file (exact-string replacement), delete_file, list_files. Resolves caller-supplied relative paths under the active account prefix, applies read-before-overwrite/edit/delete gating, and translates storage-layer results into Claude-Code-shaped tool responses

## Type

module
