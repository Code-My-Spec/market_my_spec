# MarketMySpec.Files.ReadTracker

Per-session read-state tracker that records which paths the agent has read so write_file/edit_file/delete_file can enforce the read-before-mutate gate

## Type

module
