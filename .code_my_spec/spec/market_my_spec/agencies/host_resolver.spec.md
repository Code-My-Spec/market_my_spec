# MarketMySpec.Agencies.HostResolver

Resolves a request host to an agency account by subdomain match; validates subdomain format and uniqueness on claim; updates an agency's subdomain

## Type

module
