# 2026-05-15

A single, decisive commit carried the day, but it was the kind of commit that closes a long-running credibility gap. A morning smoke test against the live MCP server revealed an uncomfortable truth: stories 708 and 710 had shipped their LiveView admin and the underlying Engagements context, but the agent-facing MCP tools their acceptance criteria promised had quietly never been built. The tools/list endpoint returned eight tools, none of them able to touch venues or saved searches. The agent was locked out of half the system.

John closed the gap in one sweep. Nine new MCP tool modules landed under the engagements server: add, list, update, and remove for venues (story 708), and create, list, run, update, and delete for saved searches (story 710). Each followed the established SearchEngagements pattern: Anubis.Server.Component, JSON Schema declared inline, execute/2 returning a clean text response, scope threaded through frame assigns, and all repository work delegated through the public Engagements context so the MCP layer stayed thin. RunSearch deliberately mirrored the existing search_engagements envelope so the agent could treat ad-hoc and saved searches interchangeably.

The cleanup was as important as the build. Nine spex were exposed as anemic: story 708's MCP criteria had been pattern-matching scaffold tuples as PASS through try/rescue, letting the QA roll-forward green them without the tools existing at all. Story 710's spex had been poking SavedSearchesRepository directly instead of exercising the agent surface their gherkin called out. All nine were rewritten against the Anubis testing recipe, hitting execute/2 and decoding the real JSON envelope before asserting against the database.

A small sidebar nav tweak in layouts.ex added Venues and Saved searches links between Accounts and Integrations, gated on an active account scope. The MarketingStrategyServer registered all nine tools, and the live wire now served seventeen. A curl-driven round trip from add_venue through delete_search confirmed every envelope. Spex finished 259 green, non-spex 168 green.

A focused, surgical day: one commit, one architectural hole closed, and a quiet reminder that passing spex are not the same as working features.
