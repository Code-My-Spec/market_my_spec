# Qa Result

## Status

partial

## Scenarios

### Scenario 1 — Touchpoints index loads for authenticated user

fail

Navigated to `http://127.0.0.1:4008/accounts/ad7ee4c0-bb95-45cf-adab-0dd31acd2496/touchpoints`. The running server returned `Phoenix.Router.NoRouteError` — the route `/accounts/:account_id/touchpoints` is not registered in the server's compiled router. The route IS present in the router source file at line 111, but the server on port 4008 predates story 716 and has not been restarted with the new compiled code.

Screenshot: `.code_my_spec/qa/716/screenshots/716-05-route-not-found.png`

### Scenario 2 — Empty state shows placeholder message

partial — not reached

Blocked by Scenario 1. The touchpoints index route does not exist in the running server.

### Scenario 3 — Touchpoint Show page loads with all fields

partial — not reached

Blocked by Scenario 1.

### Scenario 4 — Mark Posted form is present for staged/abandoned touchpoints

partial — not reached

Blocked by Scenario 1.

### Scenario 5 — Mark Posted form rejects submission without a comment URL

partial — not reached

Blocked by Scenario 1.

### Scenario 6 — Mark Posted form succeeds with a valid comment URL

partial — not reached

Blocked by Scenario 1.

### Scenario 7 — Abandon action transitions state to abandoned

partial — not reached

Blocked by Scenario 1.

### Scenario 8 — State filter tabs filter the touchpoints list

partial — not reached

Blocked by Scenario 1.

### Scenario 9 — Touchpoint row links to Show page

partial — not reached

Blocked by Scenario 1.

### Scenario 10 — Delete action removes the touchpoint

partial — not reached

Blocked by Scenario 1.

### Scenario 11 — State badge classes match state values

partial — not reached

Blocked by Scenario 1.

### Scenario 12 — Cross-account access returns not_found

partial — not reached

Blocked by Scenario 1.

### Scenario 13 — Angle field appears on Show page when present

partial — not reached

Blocked by Scenario 1.

### Scenario 14 — Abandoning preserves angle and body

partial — not reached

Blocked by Scenario 1.

### Scenario 0 — Authentication and account navigation

pass

Successfully signed in via magic-link at `http://127.0.0.1:4008/users/log-in/<token>`, clicked "Log me in only this time", and landed on the home page as `qa@marketmyspec.test`. Navigated to `/accounts` and confirmed the QA user's accounts are listed with IDs visible in rendered HTML. The threads index at `/accounts/ad7ee4c0-bb95-45cf-adab-0dd31acd2496/threads` loaded and showed "No threads have been ingested yet." with a "Touchpoints" column header.

Screenshot: `.code_my_spec/qa/716/screenshots/716-03-logged-in-home.png`
Screenshot: `.code_my_spec/qa/716/screenshots/716-06-threads-index.png`

### Scenario 0b — Source code and migration review

pass

Code inspection confirmed:
- `Touchpoint` schema (`lib/market_my_spec/engagements/touchpoint.ex`) has `state` Ecto.Enum field with values `[:staged, :posted, :abandoned]` and optional `angle` string field — matches criterion 6331 and 6332.
- `staged_changeset/2` sets state to `:staged` by default and accepts `angle` — matches criterion 6334 and 6354.
- `update_changeset/2` validates `comment_url` and `posted_at` required when transitioning to `:posted` — matches criterion 6335 and 6356.
- `TouchpointsRepository` has `list_touchpoints_for_thread/2` scoped by both `account_id` and `thread_id` (cross-account isolation) — matches criterion 6340 and 6360.
- `engagement_summary/2` reads `latest.state` from the state column directly, not inferred from `posted_at` — matches criterion 6342 and 6361.
- `TouchpointLive.Show` handles `mark_posted` event via `Engagements.update_touchpoint/3`, same context function as the MCP `UpdateTouchpoint` tool — matches criterion 6341 and 6358.
- `TouchpointLive.Show` renders `[data-test='mark-posted-form']` for non-posted touchpoints, `data-test="abandon-button"` for non-abandoned touchpoints — correct per spec.
- Router lines 111-112 contain the touchpoint routes within the authenticated `live_session :require_authenticated_user` block.
- Migration `20260516093000_recreate_touchpoints_with_uuid_pk.exs` gives touchpoints a UUID binary_id primary key with `state` and `angle` columns.
- Migration `20260516091000_add_state_angle_to_touchpoints.exs` adds `state` and `angle` to existing tables.
- All 22/22 BDD spex passed (committed in 263cb9b per task context).

## Evidence

- `.code_my_spec/qa/716/screenshots/716-01-pending-migration.png` — Initial state: server 4008 showing pending migration error on first visit
- `.code_my_spec/qa/716/screenshots/716-02-login-attempt.png` — Magic-link confirmation page for qa@marketmyspec.test
- `.code_my_spec/qa/716/screenshots/716-03-logged-in-home.png` — Logged in, home page rendering for qa@marketmyspec.test
- `.code_my_spec/qa/716/screenshots/716-04-touchpoints-index.png` — Attempt to navigate to touchpoints index (shows route error)
- `.code_my_spec/qa/716/screenshots/716-05-route-not-found.png` — Phoenix router NoRouteError for /accounts/:account_id/touchpoints
- `.code_my_spec/qa/716/screenshots/716-06-threads-index.png` — Threads index page loaded (confirming app works on 4008)
- `.code_my_spec/qa/716/screenshots/716-07-sidebar-nav.png` — Sidebar navigation without Touchpoints link (stale server)

## Issues

### Dev server on port 4008 predates story 716 — touchpoint routes unreachable

#### Severity
HIGH

#### Scope
QA

#### Description

The dev server running on port 4008 does not have the story 716 code compiled in. Attempting to navigate to `/accounts/:account_id/touchpoints` returns a `Phoenix.Router.NoRouteError` listing all available routes — the touchpoint routes (`/accounts/:account_id/touchpoints` and `/accounts/:account_id/touchpoints/:touchpoint_id`) are absent from the running server's route table.

The router source at `lib/market_my_spec_web/router.ex` lines 111-112 shows the routes correctly added. The server on port 4008 was also initially showing a `Phoenix.Ecto.PendingMigrationError` for `MarketMySpec.Repo` — indicating migration `20260516093000_recreate_touchpoints_with_uuid_pk.exs` (story 716) has not been applied to the dev database.

The server needs to be stopped, `mix ecto.migrate` run, and `PORT=4008 mix phx.server` restarted before LiveView testing of story 716 features can proceed.

Reproduction:
1. Navigate to `http://127.0.0.1:4008/accounts/<any_account_id>/touchpoints`
2. Observe `Phoenix.Router.NoRouteError` — route not in compiled router
3. Check router source at `lib/market_my_spec_web/router.ex` — routes exist in source but not in running binary

All 22 BDD spex passed against the test env (confirmed in task context), but the dev server cannot exercise the LiveView surfaces until restarted.
